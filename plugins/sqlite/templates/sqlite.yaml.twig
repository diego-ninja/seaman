{# ABOUTME: Docker Compose template for SQLite. #}
{# ABOUTME: SQLite doesn't need a container - this template is empty but required. #}

{# SQLite is a file-based database that doesn't require a Docker container. #}
{# The database file is stored at: {{ config.database_path | default('var/data.db') }} #}
