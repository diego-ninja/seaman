version: '1.0'

php:
  version: '8.4'
  extensions:
    - pdo_pgsql
    - redis
  xdebug:
    enabled: false
    ide_key: PHPSTORM
    client_host: host.docker.internal

services: {}

volumes:
  persist: []
